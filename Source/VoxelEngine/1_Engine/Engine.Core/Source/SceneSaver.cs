using Arch.Core;
using VoxelEngine.Core;
using VoxelEngine.IO;

using System.Reflection;

namespace VoxelEngine.Testing;

public sealed class SceneSaver
{

    // public void Save(Scene scene, string path, ISerializer serializer)
    // {

    //     serializer.Write(scene.Name);
    //     serializer.Write(scene.ID);

    //     World world = scene.World;

    // }
    // public Scene Load(Universe universe, string path, ISerializer serializer)
    // {
    //     string name = serializer.ReadString();
    //     Guid id = serializer.ReadGuid();

    //     Scene scene = new(universe, name, id);

    //     return scene;
    // }

}